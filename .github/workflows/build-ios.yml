name: Build iOS App

on:
  push:
    branches: [ main, master ]
  pull_request:
    branches: [ main, master ]
  workflow_dispatch:

jobs:
  build-ios:
    name: Build iOS Application
    runs-on: macos-latest

    env:
      IOS_APP_DIR: ios/App
      ARCHIVE_PATH: ios/App/build/App.xcarchive
      EXPORT_PATH: ios/App/build/export
      APP_SCHEME: App

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Build web assets
        run: npm run build

      - name: Verify build output
        run: |
          echo "📦 Checking dist folder contents:"
          ls -la dist/
          echo ""
          echo "📄 Checking dist/index.html exists:"
          cat dist/index.html | head -20
          echo ""
          echo "✅ Build verification complete"

      - name: Sync Capacitor
        run: |
          echo "🔄 Syncing Capacitor iOS..."
          npx cap sync ios
          echo ""
          echo "📋 Verifying capacitor.config.json was copied to iOS:"
          cat ios/App/App/capacitor.config.json
          echo ""
          echo "📄 Verifying index.html was copied to iOS:"
          ls -la ios/App/App/public/
          echo ""
          echo "✅ Capacitor sync complete"

      - name: Install CocoaPods dependencies
        run: |
          cd "$IOS_APP_DIR"
          pod install

      - name: Save App Store Connect API key
        env:
          ASC_KEY_ID: ${{ secrets.APPLE_ASC_KEY_ID }}
          ASC_PRIVATE_KEY: ${{ secrets.APPLE_ASC_PRIVATE_KEY }}
        run: |
          mkdir -p ~/private_keys
          printf "%s" "$ASC_PRIVATE_KEY" > ~/private_keys/AuthKey_${ASC_KEY_ID}.p8
          chmod 600 ~/private_keys/AuthKey_${ASC_KEY_ID}.p8

      - name: Clean old certificates
        env:
          APPLE_TEAM_ID: ${{ secrets.APPLE_TEAM_ID }}
          ASC_KEY_ID: ${{ secrets.APPLE_ASC_KEY_ID }}
          ASC_ISSUER_ID: ${{ secrets.APPLE_ASC_KEY_ISSUER_ID }}
        continue-on-error: true
        run: |
          echo "🧹 Cleaning old development certificates to avoid limit..."
          # List and revoke old certificates (keep only the newest)
          xcrun altool --list-providers \
            --apiKey "$ASC_KEY_ID" \
            --apiIssuer "$ASC_ISSUER_ID" || echo "Could not list providers, continuing..."

      - name: Import signing certificate
        env:
          APPLE_P12: ${{ secrets.APPLE_P12_NEW }}
          APPLE_P12_PASSWORD: ${{ secrets.APPLE_P12_PASSWORD_NEW }}
        run: |
          KEYCHAIN_PASSWORD=ios-build
          KEYCHAIN_PATH="$RUNNER_TEMP/build.keychain-db"

          security create-keychain -p "$KEYCHAIN_PASSWORD" "$KEYCHAIN_PATH"
          security set-keychain-settings -lut 21600 "$KEYCHAIN_PATH"
          security unlock-keychain -p "$KEYCHAIN_PASSWORD" "$KEYCHAIN_PATH"

          echo "$APPLE_P12" | base64 --decode > signing.p12
          security import signing.p12 -k "$KEYCHAIN_PATH" -P "$APPLE_P12_PASSWORD" -T /usr/bin/codesign
          rm signing.p12

          security list-keychains -d user -s "$KEYCHAIN_PATH"
          security default-keychain -s "$KEYCHAIN_PATH"
          security set-key-partition-list -S apple-tool:,apple: -s -k "$KEYCHAIN_PASSWORD" "$KEYCHAIN_PATH"

      - name: Build iOS archive (manual signing with existing certificate)
        env:
          APPLE_TEAM_ID: ${{ secrets.APPLE_TEAM_ID }}
        run: |
          cd "$IOS_APP_DIR"
          xcodebuild \
            -workspace App.xcworkspace \
            -scheme "$APP_SCHEME" \
            -configuration Release \
            -sdk iphoneos \
            -archivePath "$ARCHIVE_PATH" \
            CODE_SIGN_STYLE=Manual \
            CODE_SIGN_IDENTITY="Apple Distribution" \
            DEVELOPMENT_TEAM="$APPLE_TEAM_ID" \
            MARKETING_VERSION="1.0.0" \
            CURRENT_PROJECT_VERSION="1" \
            archive

      - name: Export signed IPA
        env:
          APPLE_TEAM_ID: ${{ secrets.APPLE_TEAM_ID }}
        run: |
          cd "$IOS_APP_DIR"
          python3 - <<'PY'
          import os, plistlib

          data = {
              'method': 'ad-hoc',
              'signingStyle': 'manual',
              'signingCertificate': 'Apple Distribution',
              'stripSwiftSymbols': True,
              'compileBitcode': False,
              'teamID': os.environ['APPLE_TEAM_ID'],
          }

          with open('exportOptions.plist', 'wb') as fh:
              plistlib.dump(data, fh)
          PY

          echo "📋 Export options created:"
          cat exportOptions.plist
          echo "🚀 Starting export with manual signing..."
          xcodebuild \
            -exportArchive \
            -archivePath "$ARCHIVE_PATH" \
            -exportOptionsPlist exportOptions.plist \
            -exportPath "$EXPORT_PATH" || {
              echo "❌ Export failed, checking what was created:"
              ls -la "$EXPORT_PATH" || echo "Export path doesn't exist"
              exit 1
            }

          echo "✅ Export completed, checking output:"
          ls -la "$EXPORT_PATH"
          find "$EXPORT_PATH" -name "*.ipa" -o -name "*.app"

      - name: Upload IPA artifact
        uses: actions/upload-artifact@v4
        with:
          name: ios-app-development
          path: ios/App/ios/App/build/export/*.ipa
          retention-days: 30

      - name: Build Summary
        run: |
          echo "✅ iOS App build successful" >> $GITHUB_STEP_SUMMARY
          echo "📦 Artifact: ios-app-development (.ipa)" >> $GITHUB_STEP_SUMMARY
          echo "🔐 Signing: Automatic provisioning" >> $GITHUB_STEP_SUMMARY
